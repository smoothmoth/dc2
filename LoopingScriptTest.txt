for d in *; do cd $d; ls | grep -v "metropolitan" | xargs echo; cd ..; done
read -p "These files will be PERMAMENTLY deleted. Do you want to proceed? (yes/no) " yn
case $yn in 
	yes ) echo files are being removed;;
	no ) echo exiting...;
		exit;;
	* ) echo invalid response \(type \"yes\" or \"no\"\) \- exiting;
		exit 1;;

esac
for d in *; do cd $d; ls | grep -v "metropolitan" | xargs echo; cd ..; done